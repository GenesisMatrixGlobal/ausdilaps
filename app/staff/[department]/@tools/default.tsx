/** Shown when the Tools slot has nothing to match — i.e. a hard load straight
 *  onto /training. The pane is hidden in that state anyway; toggling to Tools
 *  soft-navigates and the slot fills in. */
export default function ToolsSlotDefault() {
  return null;
}
